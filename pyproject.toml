[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-template-ops"
version = "0.1.0"
description = "Agentic AI Software Template & Model Update Tool for RHDH"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
classifiers = ["License :: OSI Approved :: Apache Software License"]
dependencies = [
    "huggingface-hub>=0.34.0",  # provides the `hf` CLI used by hf_model_agent
    "requests>=2.31.0",
    "click>=8.1.7",
    "tabulate>=0.9.0",
    "pydantic>=2.6.0",
    "packaging>=24.0",
]

[project.scripts]
agentic-template-ops = "agentic_template_ops.cli:cli"

[tool.setuptools.packages.find]
include = ["agentic_template_ops*"]
